import { TitleCustomPipePipe } from './title-custom-pipe.pipe';

describe('TitleCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
